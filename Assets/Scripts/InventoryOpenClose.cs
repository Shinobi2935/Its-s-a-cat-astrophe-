using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

public class InventoryOpenClose : MonoBehaviour
{
    public GameObject inventario;
    public PlayerController playerController = null;
    private void Update()
    {
        //Keyboard kb = InputSystem.GetDevice<Keyboard>();
        //if (kb.mKey.wasPressedThisFrame)
        //{
        /*if(playerController != null)
        {
            if (inventario)
            {
                inventario.SetActive(playerController.GetIsInventoryShown());
            }
        }
            if (inventario.activeSelf == false)
            {
                //InventoryManager.Instance.inventoryItems = new List<InventoryItemController>();
            }
            else
            {
                InventoryManager.Instance.ListItems();
            }*/
        //}
    }
}
